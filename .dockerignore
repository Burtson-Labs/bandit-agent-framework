# Keep the build context lean. Sources and every package.json stay in —
# pnpm needs the full workspace manifest set to resolve the lockfile —
# but installed modules and build outputs are rebuilt inside the image.
**/node_modules
**/dist
.git
.github
**/*.vsix
**/.DS_Store
