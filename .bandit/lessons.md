# Learned lessons

<!-- Auto-generated by Bandit from past runs — durable facts about THIS repo.
     Machine-distilled and lower-trust than BANDIT.md: edit freely, or clear
     with `/lessons clear`. Bandit reads this back on future turns. -->
- Eval fixture `bandit.release_touches_all_three` is failing (0/3): A "bump to X.Y.Z" prompt must edit cli + extension + chart — treat that description as a standing rule in this repo until the fixture passes again.
- Eval fixture `context_reuse.artifact_revision` is failing (0/3): Revising a just-published artifact edits from context instead of re-reading the file it was handed — treat that description as a standing rule in this repo until the fixture passes again.
- Eval fixture `edit.changelog_append` is failing (0/3): Append a new CHANGELOG entry in the existing format via targeted edit — treat that description as a standing rule in this repo until the fixture passes again.
- Eval fixture `edit.json_setting_change` is failing (0/3): Flip one boolean in a JSON config via targeted edit, not a rewrite — treat that description as a standing rule in this repo until the fixture passes again.
- Eval fixture `native_tools.multi_file_doc_add` is failing (0/3): Capable model + native tools: add XML docs to two C# controllers in ≤4 iterations — treat that description as a standing rule in this repo until the fixture passes again.
- Eval fixture `restraint.no_tools_needed` is failing (1/3): A general-knowledge question is answered directly with no tool calls — treat that description as a standing rule in this repo until the fixture passes again.
