# Standalone project: keep the demo pipeline out of the monorepo's
# pnpm workspace / turbo graph. `pnpm install` here installs only this
# package's deps with its own lockfile.
ignore-workspace=true
